# Policy-Based Routing Configuration
# This file is shared between Vagrantfile and setup_pbr.sh

# VM Configuration
UES_VM_IP=192.168.121.50      # VM 的 IP 地址
UES_VM_HOSTNAME=ues-vm       # VM 主機名 (lowercase, valid for libvirt)
UES_VM_MEMORY=4096             # VM 記憶體 (MB)
UES_VM_CPUS=4                  # VM CPU 數量

# free5GC VM Configuration
FREE5GC_VM_IP=192.168.121.40
FREE5GC_VM_HOSTNAME=free5GC
FREE5GC_VM_MEMORY=4096
FREE5GC_VM_CPUS=4

# Network Interface Configuration
ENO2_INTERFACE=eno2            # 主機網卡名稱
ENO2_IP=10.200.0.2/16          # 主機網卡 IP (CIDR)
ENO2_IP_ONLY=10.200.0.2        # 主機網卡 IP (僅 IP)
GATEWAY_IP=10.200.0.1          # 網關 IP

# Routing Configuration
DEST_NETWORK=10.0.0.0/8      # 目標網段
RT_TABLE_NAME=ues_routing      # 路由表名稱
RT_TABLE_ID=100                # 路由表 ID

# PBR Mode Configuration
# NETWORK_MODE: 選擇 PBR 模式
#   - "specific" : 只允許特定的 VM (UES_VM_IP 和 FREE5GC_VM_IP) [預設，較安全]
#   - "subnet"   : 允許整個私有網段 (VAGRANT_PRIVATE_NETWORK) [較簡便]
NETWORK_MODE=specific          # 預設: specific (可改為 subnet)

# Vagrant Private Network (用於 subnet 模式)
VAGRANT_PRIVATE_NETWORK=192.168.121.0/24  # Vagrant/libvirt 預設私有網段
